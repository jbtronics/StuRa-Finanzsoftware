{% extends "@!DHAuditor/layout.html.twig" %}

{#  Replace CDN stylesheets and javascripts with our local versions #}

{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset("assets/css/bootstrap.css") }}">
    <link rel="stylesheet" href="{{ asset("assets/css/fa-all.css") }}">

    {#  Remove everything from the old block except the explicit <style> CSS #}
    {{ parent() | striptags('<style>') | raw }}
{% endblock %}

{% block javascripts %}
    <script src="{{ asset("assets/js/jquery.slim.js") }}"></script>
    <script src="{{ asset("assets/js/bootstrap.js") }}"></script>
{% endblock %}