vich_uploader:

    mappings:
        payment_orders_form:
            upload_destination: '%kernel.project_dir%/uploads/payment_orders/testing/forms'
            namer:  App\Services\Upload\PaymentOrderFileNamer

            inject_on_load: true

        payment_orders_references:
            upload_destination: '%kernel.project_dir%/uploads/payment_orders/testing/references'
            namer:  App\Services\Upload\PaymentOrderFileNamer

            inject_on_load: true
